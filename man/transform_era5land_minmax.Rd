% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform_era5land_minmax.R
\name{transform_era5land_minmax}
\alias{transform_era5land_minmax}
\title{Monthly min / max 2 m temperature from ERA5-Land hourly NetCDFs}
\usage{
transform_era5land_minmax(
  input_dir,
  output_dir,
  years,
  nc_template = "era5land_\%d\%02d.nc",
  month_min_template = "era5land_monthly_min_\%d\%02d.tif",
  month_max_template = "era5land_monthly_max_\%d\%02d.tif",
  annual_min_template = "era5land_annual_minimum_temperature_\%d.tif",
  annual_max_template = "era5land_annual_maximum_temperature_\%d.tif"
)
}
\arguments{
\item{input_dir}{Directory holding \verb{<year>/<nc_template>} NetCDFs.}

\item{output_dir}{Directory for per-month and annual GeoTIFFs. A
\verb{<year>/} subdirectory is created for the monthly caches.}

\item{years}{Integer vector of years to process.}

\item{nc_template}{\code{sprintf} template for the input NetCDF basename;
receives \verb{(year, month)}. Defaults to the convention used by
\code{\link[=extract_era5land]{extract_era5land()}}.}

\item{month_min_template, month_max_template}{\code{sprintf} templates for the
cached monthly min and max GeoTIFFs; each receives \verb{(year, month)}.}

\item{annual_min_template, annual_max_template}{\code{sprintf} templates for
the 12-layer annual stacks; each receives \code{(year)}.}
}
\value{
Character vector of annual stack paths written (invisibly).
}
\description{
For each (year, month) NetCDF produced by \code{\link[=extract_era5land]{extract_era5land()}} — which
contains six variables interleaved — pulls out only the \code{t2m} layers,
converts to degC, reduces to daily min and max with \code{\link[terra:tapp]{terra::tapp()}}, then
reduces those to a single monthly min and a single monthly max raster.
Per-month results are cached as GeoTIFFs; once all twelve months for a
year are present, a 12-layer annual stack is written.
}
\details{
The input is the multi-variable NetCDF from \code{extract_era5land()}, so the
\code{t2m} layers are selected via the \verb{t2m_valid_time=<unix>} layer-name
convention rather than by raster index.
}
\examples{
\dontrun{
transform_era5land_minmax(
  input_dir  = "data/climate/era5land/input/nc",
  output_dir = "data/climate/era5land/output/minmax",
  years      = 2014:2017
)
}
}
