% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform_era5land_monthly.R
\name{transform_era5land_monthly}
\alias{transform_era5land_monthly}
\title{Transform an ERA5-Land monthly-means NetCDF into a Parquet}
\usage{
transform_era5land_monthly(
  input_nc,
  output_path,
  dem_tif,
  version = "v0.1.0",
  compression = "zstd",
  metadata = NULL
)
}
\arguments{
\item{input_nc}{Path to the monthly-means NetCDF.}

\item{output_path}{Output Parquet path. Parent directory is created.}

\item{dem_tif}{Path to a DEM raster covering the AOI.}

\item{version}{Version tag embedded in the Parquet footer metadata.}

\item{compression}{Parquet compression codec.}

\item{metadata}{Named character vector embedded in the Parquet footer.
Defaults to a generic ERA5-Land monthly-means dictionary.}
}
\value{
The output Parquet path (invisibly).
}
\description{
Reads a single CDS \code{reanalysis-era5-land-monthly-means} NetCDF, converts
temperatures to degC, computes wind speed / RH / VPD, derives monthly
precipitation (mm) and shortwave totals from the per-day means (the CDS
monthly product stores \code{ssrd} and \code{tp} as means of daily totals, \strong{not}
cumulative since 00 UTC — multiply by \code{days_in_month}, do \strong{not}
de-cumulate), joins a DEM-derived \code{elevation_m}, and writes one Parquet.
}
\details{
Compared to \code{\link[=transform_era5land]{transform_era5land()}} (hourly): the input is a single file,
cell x month volume is trivial so there is no chunking, and the
de-accumulation logic does not apply.
}
\examples{
\dontrun{
transform_era5land_monthly(
  input_nc    = "data/.../era5land-kinshasa-monthly-2014-2017.nc",
  output_path = "data/.../era5land_monthly_kinshasa_2014-2017.parquet",
  dem_tif     = "RDC_Elevation_Complete.tif"
)
}
}
